package model.dto;

public class LineDto extends Dto<Integer> {
    /**
     * Creates a new instance of <code>Dto</code> with the key of the data.
     *
     * @param key key of the data. // id
     */
    public LineDto(Integer key) {
        super(key);
    }
}
